import setuptools

setuptools.setup(
    name="vibe",
    version="0.2",
    author="Muhammed Kocabas",
    description="VIBE: Video Inference for Human Body Pose and Shape Estimationh",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)
